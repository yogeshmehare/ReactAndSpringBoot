export default class User {
  constructor(id, name, profession, profilePic, fbLink) {
    this.id = id;
    this.name = name;
    this.profession = profession;
    this.profilePic = profilePic;
    this.fbLink = fbLink;
  }
}
